import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.jerryfrontedmain',
  appName: 'Jerry_Fronted-main',
  webDir: 'dist'
};

export default config;
