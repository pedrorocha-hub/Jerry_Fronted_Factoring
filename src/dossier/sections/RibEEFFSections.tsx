import React from 'react';
import RibEeffForm from '@/pages/RibEeffForm';

/**
 * Este componente actúa como un contenedor o sección dentro de la vista de Dossier,
 * mostrando el formulario completo de gestión de RIB EEFF para un RUC específico.
 * Reutiliza el componente RibEeffForm existente para mantener la consistencia.
 */
const RibEEFFSections = () => {
  return <RibEeffForm />;
};

export default RibEEFFSections;